[pytest]
testpaths = tests
norecursedirs =
    temp_files
    **/__pycache__
addopts = -ra
markers =
    e2e_real_file: Opt-in integration tests that mutate real repo-tracked files and restore them.
    live_stack: Opt-in integration tests that target the running Docker stack over localhost.
